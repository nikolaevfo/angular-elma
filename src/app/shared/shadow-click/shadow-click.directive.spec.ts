import { ShadowClickDirective } from './shadow-click.directive';

describe('ShadowClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowClickDirective();
    expect(directive).toBeTruthy();
  });
});
